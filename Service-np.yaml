apiVersion: v1
kind: Service
metadata:
  name: project-k8s
spec:
  type: LoadBalancer 
  selector:
    app: project-k8s
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000 #container port
    nodePort: 30080
